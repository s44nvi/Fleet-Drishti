# Copy to .env.local and adjust if your backend isn't on the default port.
# Never commit .env.local.

# Base URL of the Fleet-Drishti backend (see ~/Downloads/backend).
VITE_API_BASE_URL=http://127.0.0.1:8000
